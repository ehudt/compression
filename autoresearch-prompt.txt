Goal: run autonomous compression research in this repo to improve the two key metrics of zstd_rs: compression ratio and throughput.

Use the repo-local skill compression-autoresearch by reading .codex/skills/compression-autoresearch/SKILL.md first.

You are working in /home/ehud/code/compression on the current branch unless I tell you otherwise.

Required workflow:
1. Read README.md, benches/compression.rs, tests/acceptance.rs, tests/integration.rs, and .codex/skills/compression-autoresearch/SKILL.md.
2. Create an untracked results.tsv and establish a baseline with:
   cargo bench --bench compression > bench.log 2>&1
   cargo test --test acceptance -- --nocapture > acceptance.log 2>&1
3. Iterate autonomously: make one focused change, commit it, rerun the fast bench, run acceptance tests for promising changes, and keep or discard based on the skill criteria.
4. Use profiling and full benchmark sweeps when the fast signal is ambiguous.
5. Continue indefinitely until interrupted. Do not ask whether to continue.

Constraints:
- Prioritize real improvements in ratio and throughput together.
- Do not hide regressions by editing benches or tests.
- Keep results.tsv, logs, and profile artifacts uncommitted.
- Stay within this repo only.

Research focus:
Start with the highest-leverage compression internals: src/encoder/lz77.rs, src/encoder/block.rs, src/huffman.rs, src/fse.rs, and src/frame.rs. Prefer small, explainable experiments over broad rewrites.
